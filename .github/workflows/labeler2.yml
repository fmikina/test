name: Labeler2
on: [check_run, pull_request_review]

jobs:
  label:

    runs-on: ubuntu-latest

    steps:
    - name: Merge pull requests
      uses: fmikina/automerge-action@v0.3.9
      env:
        GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
