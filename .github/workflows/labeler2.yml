name: Labeler2
on: [pull_request, pull_request_review, status]

jobs:
  label:

    runs-on: ubuntu-latest

    steps:
    - name: Merge pull requests
      uses: fmikina/automerge-action
      env:
        GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
