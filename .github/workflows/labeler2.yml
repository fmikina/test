name: Labeler2
on: [check_run, pull_request, pull_request_review, status]

jobs:
  label:

    runs-on: ubuntu-latest

    steps:
    - name: Merge pull requests
      uses: fmikina/automerge-action@v0.3.6
      env:
        GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
