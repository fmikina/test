name: Labeler2

on: push

jobs:
  label:

    runs-on: ubuntu-latest

    steps:
    - name: Merge pull requests
      uses: fmikina/automerge-action@v0.4.9
      env:
        GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
